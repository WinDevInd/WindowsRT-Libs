<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestingLibApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="using:JISoft.Controls"
    x:Class="TestingLibApp.BlankPage1"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Margin="16,0" Background="#717171">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>
            <Controls:HTMLViewer  FontSize="16" MaxLines="20" TextTrimming="CharacterEllipsis" FontFamily="Segoe UI"
            x:Name="HtmlView" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.RowSpan="2" Grid.ColumnSpan="2"
                                  Margin="0,0,16,0"/>
            <HyperlinkButton Content="more" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                         Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Grid.ColumnSpan="2"  Margin="16,-16,0,0"/>

        </Grid>
    </ScrollViewer>
</Page>
